enum TextWrap
{
    // creates a dash at the end
    DASH,
    // creates three dots
    DOTS,
    // just hides the overflow
    HIDDEN,
    // Wraps words without separator
    WRAP,
    // only full words will be printed
    FULL,
}
